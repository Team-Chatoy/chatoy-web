import { createStyles } from "@hope-ui/core";

export const centerBox = createStyles({
  baseStyle: {
    pos: "absolute",
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    m: "auto",
  },
})();
